# include <stdio.h>
// # include <string.h>
int main(int argc, char *argv[]) {
/*
Please add your statements here
*/
    // char *token, *ret;
    // token = strtok(argv[0], "\\");
    // do
    // {
    //     ret = token;
    // }
    // while (token = strtok(NULL, "\\"));
    // ret = strtok(ret, ".");
    printf("%s says Hello World!\n", argv[0]);
    return(0);
}